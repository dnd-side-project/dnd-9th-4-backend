# application-prod.yml (실제 배포 환경 설정)
server:
  port: 8000

spring:
  datasource:
    url: jdbc:mysql:// dnd-health.cade1afm85pd.ap-northeast-2.rds.amazonaws.com:3306/dnd-health?useSSL=false
    username: admin
    password: dnd12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    generate-ddl: false
    show-sql: false
    properties:
      hibernate:
        format_sql: false
